# Project coding rules

- Source files must not exceed 300 lines.
- Split oversized files by responsibility; do not compress formatting merely to reduce the line count.
- Run `npm.cmd run lint`, `npm.cmd run test`, and `npm.cmd run build` after structural changes.
- Keep Traditional Chinese UI text encoded as UTF-8.
